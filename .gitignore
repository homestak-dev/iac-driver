# Test reports (contain credentials)
test-runs/*.md
!test-runs/TEMPLATE.md
!test-runs/README.md

# Secrets - plaintext files are gitignored, only .enc files committed
secrets/*.tfvars
secrets/*.yaml
secrets/*.json
!secrets/.sops.yaml
!secrets/*.enc

# Editor/OS
*.swp
*.swo
*~
.DS_Store
